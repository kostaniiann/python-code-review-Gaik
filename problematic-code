# 1. Нарушение PEP 8 (стиль кода)
def ADD(x,y): # Нарушение именования и пробелы
    return x+y

# 2. Потенциальная ошибка деления на ноль
def divide(a, b):
    return a / b # Нет проверки b != 0

# 3. Неочевидные именования переменных
r = 0 # Что означает r?

# 4. Избыточный код
def multiply(a, b):
    c = a * b
    return c # Можно сразу return a * b

# 6. Магические числа в коде
if result > 100: # Что означает 100?
